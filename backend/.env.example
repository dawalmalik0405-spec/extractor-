# Leave DATABASE_URL blank to use backend/extractor.db (SQLite).
DATABASE_URL=

EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
MAX_UPLOAD_SIZE=26214400

# Optional absolute paths. Blank values use folders inside backend/.
UPLOAD_DIR=
QDRANT_PATH=
